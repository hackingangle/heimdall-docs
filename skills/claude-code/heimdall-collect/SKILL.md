---
name: heimdall-collect
description: >
  Heimdall 收集任务技能：把一期节目选题变成可执行、可验收、可续跑的素材收集任务。
  当用户要求"围绕某选题做调研 / 启动收集任务 / 给某一期收集素材 / 继续上次的调研"时激活。
  未指定选题或项目时，默认关联最新一期项目（API 列表第一项）；所有产出写入该项目素材库。
  流程：确定项目 → 调研简报（维度拆解）→ 分轮执行 → 缺口盘点（调研索引）。
  获取手段不做假定，由模型按当下可用能力自选；素材入库的 API 细节与质量规则由 heimdall-material 技能承担，两个技能配合使用。
tools: Read, Write, Bash, WebSearch, WebFetch
---

# Heimdall 收集任务技能

选题 → 素材库的完整编排。本技能管**任务怎么组织**；入库 API 与质量规则见 heimdall-material 技能。

**获取手段不做任何假定**：搜索、抓取、视频字幕、PDF、用户提供的文件……按当下可用能力自行选择与组合，用你认为最有效的手段拿到素材。

**所有收集产出必须关联到一个项目**——调研计划、调研索引、正文素材一律写入 `projects/{project_id}/materials`，不要悬空入库。

## 0. 确定目标项目（每次激活先做）

1. **环境检查**（见 heimdall-material §0）：确认 `HEIMDALL_API_BASE` / `HEIMDALL_API_TOKEN` 可用，并拉取项目列表：

```bash
curl -sS "$HEIMDALL_API_BASE/projects" -H "Authorization: Bearer $HEIMDALL_API_TOKEN"
```

API 按 `episode_no` 降序返回，**列表第一项 = 最新一期**。

2. **解析目标项目**（按优先级）：

| 用户输入 | 做法 |
|---|---|
| 指明项目（id / 第 N 期 / 标题关键词） | 按指明匹配 |
| 只给选题、未给项目 | 用选题标题匹配项目 `title`；无匹配则列出近期项目让用户选 |
| **未给选题也未给项目** | **默认取列表第一项（最新一期）** |

3. **确定选题 `{主题}`**：用户给了用词 → 用之；未给 → 用目标项目的 `title`。

4. **一句话确认**（未异议则继续）：「第 {episode_no} 期 · {title}（project_id={id}），主题：{主题}」。用户要换项目/选题再调整。

5. 记下 `{project_id}` 与 `{主题}`，后续步骤全部在该项目下操作。

## 1. 判断任务状态

查**该**项目素材列表，看是否已存在本主题的 `[调研计划]` 素材：

- **不存在** → 新任务，从第 2 步开始；
- **已存在** → 续跑：读取 `[调研计划]` 和最新的 `[调研索引]`，从索引中未达标的维度继续（跳到第 3 步）。续跑不依赖任何会话历史。

## 2. 调研简报（开搜之前必做）

**禁止直接拿主题去搜**——查询词同质化，覆盖面窄。先拆维度：

- 把主题拆成 **4~8 个挖掘维度**，每个维度写四件事：想挖什么 / 3~5 组不同角度的查询词 / 预期载体（text、pdf）/ **可判断的达标条件**（如「年谱全本入库」「至少 2 位弟子回忆」）。
- 人物类选题默认骨架（可增删）：生平骨架、第一手声音（自述/开示/书信）、传奇故事点（一事一挖，每事 2 个来源互证）、关系人回忆、时代背景、观点与争议。其他题材自拟。
- 附**分轮安排**：每轮 1~2 个维度，注明重点来源方向。
- 简报作为素材入库到 `{project_id}`：`type=text`，标题 `[调研计划] {主题} - {YYYY-MM-DD}`。
- **入库后停下来，向用户过目维度拆解再开跑**；用户明确说"直接跑"则跳过确认。

## 3. 分轮执行

**每轮只跑当轮维度，不要一把梭**（一次跑完后半程清洗质量会掉）。每个维度：发现 → 获取 → 速读判断相关性 → 清洗 → 入库（走 heimdall-material 规则）。

### 执行纪律

- 获取手段自选，但产出有质量底线：素材保留原文不过度摘要，一本书/一篇演讲/一篇文章/一个字幕 = 一条素材（书不按章拆条，章节用正文 Markdown 标题保留）；
- 核心著作类来源（如人物的年谱、文集）往往是定海神针，优先处理；
- 相关性速读判断，无关直接丢弃（记入调研索引）；
- 单条失败不中断，记录原因继续；反爬、付费墙来源不硬啃，记入索引留人工；
- 轮与轮之间用户可能纠偏，调整写回 `[调研计划]`（PATCH）。

## 4. 缺口盘点（每轮收尾必做）

维护一条 `[调研索引] {主题}` 素材（`type=text`）。**同一任务只有一条索引，每轮 PATCH 更新，不要新建**。内容：

1. **维度状态表**（对照计划逐维度）：

```markdown
| 维度 | 状态 | 已收素材 | 缺口与下一轮建议 |
|---|---|---|---|
| 生平骨架 | ✅ 达标 | 年谱全本 ×1 | — |
| 关系人回忆 | 🟡 部分覆盖 | 弟子访谈 ×1 | 还差其他关系人方向 |
| 观点与争议 | ⬜ 未开始 | — | 第三轮处理 |
```

2. **失败/放弃清单**：URL + 原因；
3. **人工补充建议**。

## 5. 终止条件与收尾

各维度全部 ✅（或注明放弃原因）即任务完成——不是"感觉差不多了"。最后一次更新索引后向用户汇报：各维度收录统计、类型分布、遗留人工事项。
